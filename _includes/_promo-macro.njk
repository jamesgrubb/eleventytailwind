{% macro promo(class, description, price) %}
{% from "./_macro-button.njk" import button %}

        <article class="md:w-5/12 bg-red-300 flex flex-col mt-8 promo-box">
          <div class="p-4">
            <h2 class="relative flex items-baseline text-gold bg-teal-400 text-5xl font-display yoga">{{ class }}{% include "_yoga.njk" %}</h2>
            <p class="mt-5">{{ description }}</p>
          </div>
          <div class="-mx-4 flex flex-grow py-20 justify-center flex-wrap px-20 sm:px-10 bg-teal-500 mt-auto promo-cta">
            <div class="w-full sm:w-1/2 md:w-full">
            <div class="w-full flex items-baseline justify-between"><span class="uppercase tracking-widest">
                from</span><span class="text-3xl">£{{ price }}</span>
            </div>
            
            {{ button(text='inquire') }}
            </div>            
          </div>
    </article>
{% endmacro %}
